#include <stdio.h>
int main()
{
  int num,i,sum=0;
  printf("enter the number\n");
  scanf("%d",&sum);
  for(i=1;i<=num;i++)
  sum=sum+i;
  printf("the sum of n numbers = %d",sum);
  return 0;
}
